import type { CSSProperties } from "react";

/** Fundo escuro com grade de pontos em violeta (landing + auth). */
export const marketingSurfaceStyle: CSSProperties = {
  backgroundImage:
    "radial-gradient(rgba(139, 92, 246, 0.14) 2px, transparent 2px), radial-gradient(rgba(124, 58, 237, 0.1) 2px, transparent 2px)",
  backgroundSize: "100px 100px",
  backgroundPosition: "0 0, 50px 50px",
};
